# Supabase — these are SAFE to expose in the browser. Publishable keys are
# scoped by RLS policies. Paste the same values into Vercel env vars.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
